#!/usr/bin/env bash

# fzf で git branch をチェックアウト

function fgc() {
    git checkout "$(git for-each-ref refs/heads/ --format='%(refname:short)' | fzf)"
}

if [ -n "$($SHELL -c "echo ${ZSH_VERSION}")" ]; then
    fgc
fi
